const config = {
  plugins: {
    "@tailwindcss/postcss": {},
    // autoprefixer is often not needed with modern Tailwind, 
    // but keep it if you need explicit vendor prefixing for older browsers
    // autoprefixer: {},
  },
};

export default config; 