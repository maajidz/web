# Frontend Environment Variables Example
# Copy this to .env.local for local development and fill in values
# Add these variables to your Vercel project settings

# Exposed to the browser (public)
NEXT_PUBLIC_TRUECALLER_PARTNER_KEY=
NEXT_PUBLIC_TRUECALLER_PARTNER_NAME=
NEXT_PUBLIC_PHONE_EMAIL_CLIENT_ID=
NEXT_PUBLIC_BACKEND_API_URL=

# Used for HMR/Dev Server access (Optional for Vercel Production)
# Comma-separated list of allowed origins (e.g., http://localhost:3000,https://your-preview-domain.vercel.app)
NEXT_PUBLIC_ALLOWED_DEV_ORIGINS=

# URL the Truecaller backend should redirect back to (Prod/Preview frontend URL)
# Not directly used in frontend code, but conceptually needed for backend config
# Example: NEXT_PUBLIC_FRONTEND_URL=https://your-app.vercel.app 