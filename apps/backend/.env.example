# Backend Environment Variables Example
# Copy this to backend/.env for local development and fill in values
# Add these variables to your Vercel project settings

# Server Port (Vercel ignores this, uses its own)
PORT=3001

# Allowed CORS Origins (comma-separated)
# Include your Vercel production/preview domains and localhost for local dev
# Example: http://localhost:3000,https://your-app.vercel.app,https://your-preview-branch.vercel.app
ALLOWED_ORIGINS=

# Frontend URL (for redirects)
# Example (Local): http://localhost:3000
# Example (Prod): https://your-app.vercel.app
FRONTEND_URL=

# Supabase Credentials
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# JWT Secret (Use a strong, randomly generated string)
JWT_SECRET= 